name: flutter_kim_lhc
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  camera: ^0.10.5+5
  export_video_frame: ^0.0.7
  flutter:
    sdk: flutter
  image: ^4.1.3
  image_picker: ^1.0.4
  path: ^1.8.3
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  riverpod: ^2.4.4
  tflite_flutter: ^0.10.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
